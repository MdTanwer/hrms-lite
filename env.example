# HRMS Lite – production env (MongoDB Atlas). Copy to .env and set values.
# cp env.example .env

# MongoDB Atlas connection string (required)
MONGODB_URL=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB_NAME=hrms_lite

# Backend security (required in production)
SECRET_KEY=your-production-secret-key-at-least-32-characters-long

# CORS: comma-separated origins that will call the API (e.g. your frontend URL)
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Optional
FRONTEND_PORT=80
